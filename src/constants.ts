
// Pomodoro Timer Settings
export const DEFAULT_WORK_DURATION = 25; // minutes
export const DEFAULT_BREAK_DURATION = 5; // minutes
export const DEFAULT_LONG_BREAK_DURATION = 15; // minutes
export const DEFAULT_SESSIONS_BEFORE_LONG_BREAK = 4;

// Task Priority Levels
export const PRIORITY = {
  URGENT: 1,
  HIGH: 2,
  MEDIUM: 3,
  LOW: 4
};

// API URLs
export const API_ENDPOINTS = {
  STRAVA: '/api/strava',
  TASKS: '/api/tasks',
  JOURNAL: '/api/journal'
};

// App Settings
export const MAX_JOURNAL_LENGTH = 10000;
export const MAX_GOALS_PER_QUARTER = 3;
